{
  "name": "@auto/api-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Typed bridge between UI and API. Preserves semantic IDs and centralizes fetch/error handling + TanStack Query keys so pages cannot invent parallel contracts.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@auto/semantic-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
