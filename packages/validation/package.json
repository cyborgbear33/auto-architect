{
  "name": "@auto/validation",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zod input contracts for every action the API accepts (observations, diagnostic problems, repair log entries).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
