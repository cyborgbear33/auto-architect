{
  "name": "@auto/cartridges",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Declarative perception (DTC/PID -> ABox) + framing (fault class -> diagnostic Problem draft) cartridges, resolved per vehicle by engine family.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@auto/ontology": "workspace:*",
    "@auto/semantic-types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
